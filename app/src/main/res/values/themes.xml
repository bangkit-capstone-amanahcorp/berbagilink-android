<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PtAmanah" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="HomePage" parent="Base.Theme.PtAmanah">
        <item name="colorPrimary">@color/biru_toska</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="myEvent" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/cardview_light_background</item>
        <item name="colorOnPrimary">@color/black</item>
    </style>

    <style name="TopRoundedCorner" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeTopRight">16dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

</resources>